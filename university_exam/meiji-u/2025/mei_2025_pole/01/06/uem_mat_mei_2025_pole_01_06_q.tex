\documentclass[../../../../uem_mat_mei_q.tex]{subfiles}

\begin{document}
    $a$，$b$を $0<a<b$ を満たす定数とする．実数$x$の関数\\
    $f(x)=(x-a)(x-b)$ は

    \myspaceb%
    $\displaystyle%
        \int_0^b f(x)\,dx=0,\quad\int_0^b |f(x)|\,dx=9%
    $\\
    を満たすとする．\\
    このとき，$a=\myfracc{\myboxe{ア}}{\myboxe{イ}}$，%
    $b=\myfracc{\myboxe{ウ}}{\myboxe{エ}}$ である．
\end{document}
